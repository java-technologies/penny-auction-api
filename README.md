# Penny Auction backend service

## Docker Compose
To run the app with `docker-compose` execute the following commands:
> First run `set COMPOSE_CONVERT_WINDOWS_PATHS=1` (Windows only)
```sh
$ gradlew clean assemble
$ docker-compose up
```
